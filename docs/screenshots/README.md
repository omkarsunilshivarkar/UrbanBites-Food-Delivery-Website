# Screenshots

Add the following images for the main README:

| File | What to capture |
|------|-----------------|
| `menu.png` | Meal listing with search bar |
| `cart.png` | Cart modal with items |
| `checkout.png` | Checkout form or success screen |

Run the app locally (`npm run dev` + `cd backend && npm start`), take screenshots, and save them here.
